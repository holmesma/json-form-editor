define(["require", "exports"], function (require, exports) {
    var Defaults = (function () {
        function Defaults() {
        }
        Defaults.editors = {};
        return Defaults;
    })();
    return Defaults;
});
